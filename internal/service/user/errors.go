package user

const (
	ErrUsernameExists = "username already in use"
	ErrEmailExists    = "email already in use"
	ErrBothExists     = "username and email already in use"
	ErrNotFound       = "user not found"
)
